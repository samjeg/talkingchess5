// function setMovement() {
	// 	selectedPlace = chessMech.current_selected_piece
	// 	selectedPiece = chessMech.current_selected_coordinates
	// 	console.log("selected place: "+selectedPlace+" selected piece: "+selectedPiece+" url: "+window.location.href.substring());
	// }

	// setInterval(setMovement, 3000);
    
    // $.ajax({
    // 	type: "POST",
    // 	// data: JSON.stringify(chessMech.chessPiece.live_chessboard_matrix),
	   //  contentType: "application/json; charset=utf-8",
	   //  dataType: "json",
	   //  success: function(data) {alert(data);},
	   //  failure: function(errMsg) {
	   //      alert(errMsg);
	   //  }
    // });	

    // user_input_state = document.getElementById("input_state");
		// user_input_state.setAttribute("value", id);
		// chess_submit_btn = document.getElementById('chess_submit_btn');
		// chess_submit_btn.click();

	
	// chessMech = null;
	